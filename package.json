{
  "private": true,
  "scripts": {
    "build:site": "npm run fetch:github && bundle exec jekyll build && npm run optimize:assets",
    "benchmark:lighthouse": "node scripts/run-lighthouse.mjs",
    "fetch:github": "node scripts/fetch-github-repositories.mjs",
    "optimize:assets": "node scripts/optimize-assets.mjs",
    "validate:source": "node scripts/validate-source.mjs",
    "validate:source:update": "node scripts/validate-source.mjs --update-baseline",
    "serve:site": "serve _site -l 4010"
  },
  "devDependencies": {
    "@fontsource/instrument-sans": "^5.2.8",
    "@fontsource/jetbrains-mono": "^5.2.8",
    "lighthouse": "^13.0.1",
    "purgecss": "^7.0.2",
    "serve": "^14.2.5",
    "terser": "^5.43.1"
  }
}
